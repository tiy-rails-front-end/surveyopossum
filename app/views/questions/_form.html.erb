<%= form_for(@question) do |f| %>
<%= if @question.question_type== "Short Form" %>
<p>
  <%= ff.label :question_text, "Question" %><br>
  <%= ff.text_area :question_text %>
  <%= ff.check_box :_destroy %>
  <%= ff.label :_destroy, "Remove Question" %>
</p>
<% end %>
