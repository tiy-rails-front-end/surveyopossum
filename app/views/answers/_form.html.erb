<div class="field">
    <%= render 'answers/short_form', {:ff => ff, :question => question} if question.question_type == "Short Form" %>

    <%= render 'answers/multiple_choice', {:ff=> ff, :question => question} if question.question_type == "Multiple Choice" %>

    <%= render 'answers/yes_no', {:ff => ff, :question => question} if question.question_type == "Yes/No"%>
    <%= render 'answers/long_form', {:ff => ff, :question => question} if question.question_type == "Long Form"%>

</div>
