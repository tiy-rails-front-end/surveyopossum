  <%= ff.radio_button :answer_text, "yes", required: "#{"required" if @submission.answers.each {|a| a.errors}}", class: "#{question.question_type}" %>Yes
  <%= ff.radio_button :answer_text, "no", required: "#{"required" if @submission.answers.each {|a| a.errors}}", class: "#{question.question_type}" %>No
