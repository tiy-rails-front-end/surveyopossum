<% question.options.each do |o| %>
    <%= ff.radio_button :answer_text, o.name, required: "#{"required" if @submission.answers.each {|a| a.errors}}", class: "multiple choice" %>
    <%= o.name %>
<% end %>
